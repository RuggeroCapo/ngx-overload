import {PreloadOnOverDirective} from './preload-on-over.directive';

describe('PreloadOnOverDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloadOnOverDirective();
    expect(directive).toBeTruthy();
  });
});
